<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>edit</title>
</head>
<body>

    <a href='/feed'>Home</a>

    <div class="full-article"
        <h2><%= user.title %></h2>
        <p><%= user.text %></p>
        <p><%= user.create_at %></p>
    </div>

    <form action="/delete-article/<%= user._id %>" method="post">
        <button>Delete</button>
        <a href="/feed/edit/1/<%=user._id %>">edit</a>
    </form>
</body>
</html>